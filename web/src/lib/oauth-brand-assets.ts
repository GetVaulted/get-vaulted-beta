/** Static official OAuth brand assets — do not replace with generated SVGs. */
export const OAUTH_BRAND_ASSETS = {
  /** Google Identity pre-approved dark "Sign in with Google" button (PNG). */
  googleSignInButtonDark: "/brand/oauth/google-signin-dark-normal.png",
  /** Standard-color Google "G" on white (required for custom buttons). */
  googleGOnWhite: "/brand/oauth/google-g-on-white.png",
  /** Apple CDN-generated black pill "Sign in with Apple" button (PNG). */
  appleSignInButtonBlackPill: "/brand/oauth/apple-signin-black-pill.png",
  /** Apple Sign in with Apple logo-only mark (PNG). */
  appleLogoWhite: "/brand/oauth/apple-logo-white.png",
} as const;
