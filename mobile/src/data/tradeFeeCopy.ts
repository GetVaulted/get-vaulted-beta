/** Static copy for trade fee UI (no mock trade dependency). */
export const TRADE_FEE_INCLUDES_BULLETS = [
  '$2.99 Get Vaulted platform fee per party',
  'Your actual outbound shipping label cost (paid separately at checkout)',
  'Optional cash: settle off-platform, or pay on Get Vaulted (Stripe card fees apply)',
] as const;
