/** Card row shape for trade item previews (listings → UI). */
export type TradeItem = {
  id: string;
  title: string;
  imageUrl: string;
  conditionGrade: string;
  estValue: string;
  authStatus: string;
  vaultedVerified: boolean;
};
